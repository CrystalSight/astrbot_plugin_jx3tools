# JX3Tools specification

## GitHub initial hosting

2026-08-01 的目标是把当前插件作为公开 MIT 项目首次托管到
`https://github.com/CrystalSight/astrbot_plugin_jx3tools`。本轮包含完整代码审查、标准
单轮安全审查、公开仓库文档与 CI 整理，以及通过门禁后的首次 `main` 推送；不创建 GitHub
Release、版本标签、发布压缩包、AstrBot 插件市场条目或市场 PR。

本轮默认不改变用户可见指令、配置、数据或网络协议。若审查修复需要改变行为，必须先在本
规格中增加对应场景和验收标准，并把插件版本统一提升到 `v0.7.2`。不得新增运行时依赖、迁移
或删除数据，除非另行获得确认。

代码审查确认三条公开前失败路径需要修复：管理员纠正无效基础地址并重新初始化后不得继续
显示旧启动错误；远程图片触发 Pillow 解压炸弹保护时必须转换为可控的本地渲染错误；文章正文
只包含脚本、样式等被过滤内容时不得通过原始文本回退重新进入图片，而应显示正文不可用。

标准安全审查确认公开前还需收紧六类低风险边界：物品搜索的可复制名称必须过滤 URL 形态并
按展示字符上限截断；文章选择必须要求稳定的会话与发送者身份，且不同用户不能替换已有流程；
单张 RGB 画布最大高度降为 12,000 px；小药去重必须保持线性复杂度且继续保留全部唯一项；
零宽或组合字符不得让逐字符换行退化为无界二次复杂度；渲染线程被取消时，调用方必须继续
等待受控工作结束并清理预先登记的临时文件。同步脚本还必须限制资源主机、禁用重定向并校验
图片格式/尺寸，WebChat 烟测只允许回环地址。

2026-08-01 推送门禁发现 JX3API 主站已经切换到新版公开接口，但维护方仍在
`https://api.jx3api.com` 提供兼容旧 Token 的备份服务。维护者明确决定本次首次 GitHub 托管
继续采用已经完成真实验证的旧版契约；新版 OpenAPI 与新 Token 适配留到后续版本。本轮必须
恢复固定的 `POST /data/...` 路由、JSON 请求体和旧参数边界，并把新安装的默认基础地址改为
备份域名。Token/Ticket 仍只注入明确需要的端点，免费端点不得携带任何凭据；Token 只需放入
JSON 请求体，不再重复放入请求头。

备份接口当前仍会在物品与名片响应中返回指向 `www.jx3api.com` 的旧绝对图片地址，但主站同一路径
已经返回新版 JSON。仅当插件基础地址精确为 `api.jx3api.com` 时，下载链必须把该旧主机精确改写为
备份主机并保留路径与查询；其他主机不得改写，改写后的请求仍须经过既有 HTTPS、主机、重定向、
MIME、字节与像素安全校验。

2026-08-01 维护者决定首次公开提交只排除 96 张未确认再分发授权的 JX3BOX 缩略图，不调整
奇遇或百战运行时逻辑与布局。公开仓库不分发这些 PNG；现有缺图回退继续生效，两个功能的视觉
重构留到独立后续任务。

### GitHub hosting acceptance criteria

- [x] 代码审查覆盖源码、配置、测试、脚本、CI、元数据和文档；不存在未解决的 P0–P2 问题。
- [x] 配置修复后的重新初始化清除旧启动错误；图片解压炸弹和纯主动内容文章均安全降级且有
  回归测试。
- [x] 标准安全审查覆盖全部仓库文件；不存在未解决的可报告安全发现，依赖和敏感信息扫描通过。
- [x] 文章状态按稳定会话与发送者隔离；异常名称、超长画布、组合字符、同步图片、脚本 URL 和
  渲染取消路径均有安全回归测试；备份服务的旧版 POST 路由和逐端点 Token/Ticket 注入符合
  本轮兼容契约。
- [x] 代码使用 MIT License；`metadata.yaml` 的作者和仓库地址均指向 `CrystalSight`，版本在
  metadata、项目元数据和 HTTP User-Agent 中一致。
- [x] README、CHANGELOG、SECURITY 和第三方素材声明完整；字体、凭据、用户数据、缓存和构建
  产物不进入 Git。
- [x] 96 张未取得再分发依据的 JX3BOX 来源缩略图全部排除出 Git 候选范围，忽略规则和
  `THIRD_PARTY_NOTICES.md` 明确记录“不随公开仓库分发”。
- [x] 发布结构校验、compile、Ruff、Pyright、Pytest、依赖检查和匹配版本 Docker 验证全部通过。
- [ ] 用户审阅最终暂存范围并明确确认后，初始提交推送到公开仓库 `main`，GitHub Actions
  `quality` 工作流通过。

截至 2026-08-01，旧版备份契约、媒体主机兼容和全部发布门禁均已重跑通过。对
`api.jx3api.com` 的真实只读 Docker 冒烟为 0 failures，现有旧 Token/Ticket 可完成会员查询与
随机名片图片下载；完整结果只记录非敏感结构、数量和字节数。不得把 Token/Ticket 或响应中的
私人数据记录到仓库。

## Goal

为 AstrBot 4.26.6 提供自用的剑网 3 查询插件。插件只暴露已确认有用的固定
JX3API 接口，按功能定制中文字段和输出，并使用本地 Pillow 与阿里巴巴普惠体 3
生成适合手机阅读的单栏图片。查询结果、字体文件和文章正文不得交给远程文转图服务。

本轮属于高风险变更：涉及外部网络、会员凭据、第三方图片下载、临时文件和短时交互状态。
实现必须限制请求目标、按接口最小化凭据、限制响应大小、可靠清理临时图片与会话状态，
并在 AstrBot 4.26.6 / Python 3.12 Docker 环境中验证。

2026-07-19 第二轮反馈要求修复已在真实 WebChat 中复现的文章选择/超时与赤兔空数据问题，
并重新设计月历、小药、奇遇记录、名剑战绩、名剑门派、金价、物品搜索和物价的局部排版。
验收以 AstrBot 事件管线中的用户可见结果为准，不以绕过事件管线的直接方法调用代替。

2026-07-19 第三轮反馈要求彻底消除三类文章在合法选择后的错误超时提示，继续优化小药、
奇遇记录、名剑战绩和物品搜索的视觉层级，修复真实物价响应解析，并移除名剑门派指令。
验收继续以完整 AstrBot 事件生成器链路、真实 JX3API 只读响应和实际 Pillow 成图为准。

2026-07-20 第四轮反馈属于中风险渲染行为修复：超长技改正文在单图截断时必须保留已经
容纳的正文；小药真实最长名称不得形成突兀的第三行；名剑赛季卡中原位于右半边的当前积分、
排名和 MVP 必须在右半边共用同一条左侧起始线，段位和战绩保持不变。本轮不改变接口、
凭据、持久化或运行时依赖，验收以回归夹具、真实小药响应和匹配版本 Docker 成图为准。

2026-07-20 第五轮反馈属于高风险行为与渲染变更：技改在用户选择有效序号后，需要先发送
经校验的剑网 3 官网正文 URL，再发送本地正文图片；小药需要确认唐门·天罗诡道的展示项与
真实接口逐项一致；名剑战绩在只有一张赛季表现卡时复用现有角色名片接口与相同区服、角色
参数，将统计卡收窄并用名片图填充右侧。新增的名片查询和下载均为可选增强，失败时必须回退
原有完整战绩图，不新增参数、依赖、凭据或持久化数据。

2026-07-20 第六轮反馈属于中风险局部渲染调整：用户确认将单模式名片布局中的当前积分、
排名和 MVP 整列相对第五轮基线向右移动 38 px，以缩短统计圆角框右侧空白。段位、战绩、
统计框、名片图、最近战绩表和无名片回退布局均保持不变；验收以用户确认的真实数据预览、
坐标回归测试和匹配版本 Docker 成图为准。

## Non-goals

- 不修改 AstrBot Core，不新增 Pages、WebSocket 事件推送或持久化查询缓存。
- 不创建 GitHub Release、版本标签、发布压缩包或 AstrBot 插件市场条目/PR。
- 不在本次首次托管中同时支持或自动切换新版 JX3API OpenAPI；新版适配另行设计、测试和发布。
- 不保留已由用户明确移除的指令，不公开发布、提交或推送字体文件。
- 不允许聊天用户传入 URL、API 路径、Token、Ticket 或任意服务器文件路径；仅允许插件把
  JX3API 技改列表中经固定官网规则校验的所选正文 URL 作为独立回复发送。
- 不把字体、文章正文、角色资料或 API 返回值发送给远程 HTML/图片渲染服务。
- 不把字体放入容器临时层或插件源码目录。

## Commands and discovery

### Visible groups

帮助和功能列表只显示“免费、会员、其他”三类；不显示 VIP I/VIP II、稳定英文 key、
内部 API 路径、Token/Ticket 要求或图片中的服务元数据。帮助总览明确说明：
`<参数>` 为必填，`[参数]` 为可选。帮助和发现入口统一使用“指令”，不再向用户展示
“接口”一词；`/jx3 指令 全部` 一次列出全部可用指令。

精确功能名或别名搜索必须优先返回唯一功能，避免“角色”同时显示奇遇记录，或“名片”
同时显示缓存名片等无关帮助。

### Retained commands

- 免费：日常、月历、行侠、科举、新闻、公告、开服、技改、小药。
- 会员：角色、阵眼、奇遇记录、名剑战绩、金价、诛恶、百战、马场、名片、
  角色百战、今日赤兔、本周赤兔、物品搜索、物价、随机名片、解密。
- 其他：骚话、舔狗。

本轮新增移除：名剑门派。此前移除：鲜花、技能、黑市、关隘、八卦、家具、器物、区服、奇穴、
未做奇遇、近期奇遇、奇遇统计、奇遇汇总、名剑排行、
招募、师徒、沙盘、阵营事件、贴吧物价、掉落、本服榜单、资历、骗子、扶摇、拍卖、
的卢、烟花、缓存名片、全部名片、试炼排行、帮战、统战、语音。

## Functional presentation

- 日常：可选区服和 -30–30 天偏移；将
  `war/battle/orecar/school/draw/luck/card/team[0]/team[1]` 分别展示为
  大战、战场、阵营日常、门派事件、美人图、福缘宠物、小周常、武林通鉴·公共任务、
  大周常；忽略 `rescue`，列表内容仅用换行分隔。
- 月历：不接收天数参数，固定向旧版月历列表路由发送 `num=15`，即前后各 15 天。结果按真实
  周日到周六日历布局，仅保留大战和战场；单元格不重复星期，年份统一放在标题，月份只在
  跨月边界（含月初/月末）标注。大战/战场标签与具体值使用不同字号和颜色。每一周按该周
  最长内容动态计算行高，完整换行展示大战和战场，不截掉文字，也不允许文字越过单元格边界。
- 行侠：帮助明确必填参数是“指定要查询的声望社群”；结果只显示前三条，`site` 显示为
  “位置”。
- 科举：题目为必填参数，支持原文、模糊词和拼音首字母查询；帮助示例包含
  `单选题：古琴有几根弦（DXTGQYJGX）`。结果不显示正确性、索引和拼音字段。
- 开服：聊天参数只保留区服，使用旧版 `/data/status/check` 并固定发送 `type=1`；强制文字，仅返回区服和
  “开服/未开服”。
- 小药：替代“门派食物”，无参数查询全部小药；相同门派的心法按游戏门派数据顺序相邻。
  使用无显式边框的 5 列布局，第一列展示门派/心法，后四列展示小药；超过四个小药时在同一
  心法下继续分行。小药使用 `名称（属性）` 形式，门派、心法和小药文字均使用对应代表色，
  依靠颜色、留白和轻量底色分组，不使用卡片格子逐项包围。无相楼门派文字使用幽罗引主题色；
  小药文本按完整语义单元换行，禁止把闭合右括号单独留在第三行。适度缩窄门派/心法列并按
  实际文本在 20–16 px 之间选择字号，使当前真实接口的最长 `名称（属性）` 最多占两行，
  不截断、不越界；普通短名称仍保持 20 px。每个心法的展示项必须等于真实接口按完整
  `名称（属性）` 去重后的集合；四列分块只改变换行，不得受全局条数设置影响或丢项。
- 角色：合并 `zoneName-serverName` 为区服；展示角色名、角色 ID、全局 ID、门派、体型、
  帮会和阵营；不显示 `forceId/bodyId/tongId/campId`。
- 阵眼：外层 `name` 为心法，`skillName` 为阵眼名称；按 level 排序，只展示 1–6 数字序号，
  不显示“一重粗识”等名称、level 和闭阵。每重效果分段留白，避免拥挤。
- 奇遇记录：按“普通奇遇”“绝世奇遇”分组，每组三列排布无显式边框的奇遇单元。每个单元
  从上到下只显示固定奇遇图标、北京时间触发时间、奇遇名称，依靠列间距和行间距形成网格感。
  图标是首要信息，尺寸显著增大；触发时间次要、奇遇名称最弱化，但三列布局保持不变。
  一级归入普通奇遇、二级归入绝世奇遇；三级宠物奇遇和其他等级全部过滤。首次公开仓库不分发
  原 JX3BOX 图标，缺图时沿用现有回退；本轮不新增运行时素材请求或调整布局。
- 名剑战绩：按 JX3API 字段语义展示 2V2/3V3/5V5 当前积分、段位、排名、总场次、胜场、
  胜率和 MVP；三种模式使用紧凑的多列卡片而非显式表格。卡片上部左对齐放大模式名，同行
  右侧显示当前积分；下部仍将段位/排名、战绩/MVP 分为两行，每行两项。当前积分、排名和
  MVP 均保留在右半边，三者的左边缘使用同一条竖向起始线；段位、战绩及双列结构不变，并在
  需要时保留明确的胜率标签。
  只有一个赛季表现模式时，使用原查询的区服和角色名调用现有角色名片接口；名片下载成功后，
  将赛季统计圆角框收窄到内容区约六成，右侧使用等高圆角裁切名片图填充；当前积分、排名和
  MVP 在继续共用同一条左侧起始线的前提下，相对无偏移基线统一向右移动 38 px，段位、战绩
  及两行双列结构不变。存在多个模式或名片查询、下载、解码失败时使用原有完整卡片网格，
  名剑战绩本身仍正常返回，且不得显示图片 URL 或内部错误。
  最近战绩展示时间、模式、心法、胜负、积分变化和赛后积分；模式使用 `3V3` 等完整写法并与
  心法同一行。正积分变化使用高饱和红色，负积分变化使用高饱和绿色，不输出原始结构。
- 金价：固定向 API 发送 `limit=15`；日期为横轴、金币比例为纵轴，各交易平台在同一张
  多折线图上展示；空值和零值不参与曲线。折线在高分辨率图层绘制后缩小以消除锯齿；在图表
  右侧为每条曲线的最近有效数据点预留标签区，使用对应曲线颜色显示数值并自动错开重叠。
- 诛恶：不显示 ID；`map_name` 显示为地图；Unix 时间转换为北京时间。
- 百战：只在标题区显示本周开始和结束日期；主体参考 JX3BOX 百战异闻录，使用 10 列、
  隔行反向的蛇形地图，仅显示节点序号、首领头像和名称。首次公开仓库不分发原 JX3BOX 头像，
  缺图时沿用现有回退；本轮不新增运行时素材请求或调整布局。
- 马场：展示区服、服务器、各地图的全部马匹/刷新信息和备注，所有字段使用中文标签。
- 名片、随机名片：先发送“区服：大区-服务器”和“角色名”，再下载 `showAvatar`
  为受控本地临时图片直接发送，绝不回复 URL；名片下载使用独立媒体超时与有界重试，
  普通 API 请求超时不应直接导致名片图片失败。
- 角色百战：只显示区服、角色、体力、精力和技能数量。
- 今日赤兔、本周赤兔：API 的业务码 400“暂无数据”以及空对象都转换为明确的中文暂无记录提示，
  不生成没有实际内容的通用图片。
- 物品搜索：在图片之前先以纯文字逐行发送结果的 `name`，方便复制；URL 形态名称不进入纯
  文字回复，名称文本总量受展示字符上限约束，超限时添加明确截断提示。图片按商品卡片展示
  分类、子类、名称、别名、描述、数值、日期。若受控 HTTPS `view` 图片下载和校验成功，将其
  居中放在标题红色分割线与“搜索结果”之间，宽度接近“搜索结果”圆角内容框；为空或失败时
  无占位直接省略，不显示 URL。
  官方返回的 `nico.nicemoe.cn` 图片主机纳入精确允许列表，结果强制使用图片且不受文字条数
  截断影响。
- 物价：按 JX3API 文档解析多层 `list` 并扁平化实际交易记录；取消与物品搜索重复的物品摘要，
  先显示出售/收购等价格统计，再以区服、状态、价格、时间展示近期记录。若元数据或顶层的
  受控 HTTPS `view` 图片下载和校验成功，将其居中放在标题红色分割线与“价格统计”之间；为空
  或失败时无占位直接省略。忽略内部 ID、Token、source、index 和状态码，不倾倒原始 JSON。
- 解密：分类为会员，说明为“副本·一之窟解密玩法”；兼容新旧 API 字段，只以纯文字输出
  当前与下一时段的方位结果，便于复制，不显示概要、条件、倒计时或“第 1 项”。
- 骚话、舔狗：强制文字，只返回正文，不添加“内容：”前缀。

## Article selection

新闻、公告根据用户可选数量参数返回标题和日期；技改固定返回最近 5 条。正文请求参数必须
从官方文章 URL 解析：新闻兼容路径中的 `catid/id` 以及当前实际列表中的
`/announce/gg.html?id=...` 结构，公告和技改使用查询参数 `id`，不得把列表顺序号误当正文
ID。列表发出后，同一会话进入 10 秒选择期：

1. 记录统一消息来源、发起者 ID、候选项和截止时间，不保存事件或平台对象。
2. 仅发起者发送的纯数字序号可触发；其他成员不能替代选择。
3. 收到合法序号后立即回复处理进度，随后从固定的剑网 3 官网文章接口获取正文。
4. 使用安全 HTML 文本提取器忽略脚本、样式、链接、视频和外链资源，将标题、日期与正文
   渲染成一张本地图片；新闻和公告不得把文章 URL 发到聊天，技改则在正文图片前独立发送
   JX3API 列表中经固定 `https://jx3.xoyo.com` 规则校验的所选正文 URL。
5. 列表回复流发送后立即结束，使 WebChat 串行 WebSocket 接收循环能够处理后续数字消息；插件
   只保存统一消息来源并启动一个有界超时任务。10 秒到期且仍未选择时，该任务通过官方
   `Context.send_message()` 向同一会话发送一次超时通知并清除状态；不重试、不跨会话发送，
   越界、正文不可用或图片超限时给出可行动的中文错误并清除状态。

只有同时具备非空统一消息来源和非空发送者 ID 的事件才能创建或消费选择流程。每个会话同时
只允许一个选择流程；同一发起者的新流程可替换旧流程，不同发起者的新查询必须被拒绝且不能
覆盖原流程。完成选择和插件终止时都取消并等待旧超时任务，不保存事件或平台对象，不遗留任务
或重复通知。一次查询即是这条一次性超时通知的
用户授权；通知频率上限为每次查询一条、无重试，选择或重新查询即取消。数字选择处理器使用
当前事件直发两条进度，最终只向 AstrBot 结果管线 `yield` 图片或错误，避免中间结果截断后续处理。

## Local image rendering

- 画布固定 720 px 宽，单栏自适应高度；左右边距 48 px，标题 46–50 px，正文 28–32 px，
  标签 24–26 px，保证手机缩放后仍可阅读。
- 使用米白底、黛青正文、竹青和朱砂强调，信息卡占满可用宽度，不产生右侧空白。
- 标题下朱砂线从副标题最左端起，宽度按副标题实际渲染宽度动态计算；副标题换行时取最长
  一行，且不超过内容区，不再使用固定长度。
- 可选 `view` 图片位于朱砂线下方、首个内容区标题上方，按原比例缩放并居中；下载或解码失败
  时整块省略，不改变后续内容起点之外的语义。
- 正文使用 `AlibabaPuHuiTi-3-55-Regular.ttf`，标题/重点使用
  `AlibabaPuHuiTi-3-75-SemiBold.ttf`。
- 字体固定从 `data/plugin_data/astrbot_plugin_jx3tools/fonts` 读取；源码、Git 和镜像中
  不包含字体。缺失字体时图片查询降级为有界文字，并在日志中给出管理员路径提示。
- 图片写入系统临时目录，通过 AstrBot `track_temporary_local_file()` 跟踪清理。
- 单图最大高度 12,000 px，本地图片渲染并发固定为 1；超过时在末尾明确标注内容已截断，
  不拆成多张图片。文章正文按可用行逐行填充，超长段落和后续段落放不下时保留此前全部可见
  行，再绘制截断标记；正文卡片底色和边框不得覆盖已绘制正文，也不得返回空白正文栏。

## Network, credentials, and failures

- JX3API 只允许管理员配置的不含路径 HTTPS 基础地址和注册表中的固定旧版路径；新安装默认
  使用 `https://api.jx3api.com`。所有查询使用 POST、JSON 请求体和禁用重定向，不接受聊天用户
  提供的路径或方法。
- Token 只注入 `requires_token=True` 的接口；Ticket 只注入 `requires_ticket=True` 的接口。
  两者仅位于 JSON 请求体，免费端点不得携带已有 Token/Ticket；随机名片和解密需要 Token，
  阵眼和名剑战绩额外需要 Ticket。
- 对只读查询的连接中断、服务端错误、空响应和 JSON 解码失败最多重试 2 次，采用短指数退避；
  鉴权失败、参数错误、限流和响应过大不重试。
- 文章只访问固定 `https://jx3.xoyo.com/api.php`；图片只接受受信 HTTPS 主机，禁用重定向，
  校验 MIME、像素、字节上限，并拒绝 SVG 和非图片内容。普通物品图与名片图遇到超时或断流
  均执行最多 3 次有界重试；名片下载另用独立媒体超时，全部图片沿用同一安全校验链。
- 使用旧版备份基础地址时，仅将 JX3API 返回的精确 `https://www.jx3api.com` 图片主机替换为
  `https://api.jx3api.com`；路径与查询保持不变，任何其他来源不执行替换。
- 日志和用户错误不得包含 Token、Ticket、文章 URL、请求体、完整私人资料或内部异常。
- 共享 HTTP session、并发限制、每会话限流和 4 MiB 响应上限保持有效；终止可重复执行。

## Configuration and dependency

- `requirements.txt` 和项目元数据声明 `aiohttp>=3.11,<4`、`Pillow>=12,<13`。
- 配置分为通用、凭据、功能、网络、展示五组；会员开关合并为一个。AstrBot 按新 Schema
  删除已取消功能对应的 `vip1_enabled/vip2_enabled`、`image_threshold` 和 `sound` 旧键。
- 保留现有 AstrBot 配置中的 Token/Ticket；插件不复制或删除查询数据及其他用户数据。

## Acceptance criteria

### 2026-07-20 第六轮反馈

- [x] 用户确认的单模式名片布局中，当前积分、排名和 MVP 相对第五轮基线统一右移 38 px，
  三者左边缘仍严格共线，右侧文字不触碰统计框边界。
- [x] 模式名、段位、战绩、统计框与名片图的尺寸和位置不变；多模式卡片和名片不可用时的
  原有完整宽度回退布局不应用该偏移。
- [x] 已确认预览与最终插件使用同一坐标参数；新增回归测试、`validate_plugin.py`、
  `ruff check .`、`pyright`、`pytest` 全部通过。
- [x] AstrBot 4.26.6 / Python 3.12.13 Docker import、isolated load 和真实单模式名剑成图验证
  通过，临时容器和预览文件均已清理。

### 2026-07-20 第五轮反馈

- [x] 技改合法序号选择完成后，用户依次收到经校验的剑网 3 官网正文 URL 和本地正文图片；
  URL 必须来自所选列表项，新闻、公告、无效主机及无效路径不发送 URL，原进度、超时取消和
  事件终止语义保持有效。
- [x] 唐门·天罗诡道的图片展示项与同次真实 `/data/school/foods` 响应按完整
  `名称（属性）` 去重后的集合逐项一致；超过四项时继续分行，不卡在 7 项或受 `max_items`
  影响，并在 Docker 诊断结果中记录 API 原始条数、唯一条数与渲染条数。
- [x] 单模式名剑战绩在现有区服、角色参数下复用 `/data/card/record`；名片可用时赛季统计框
  收窄且右侧等高圆角名片填充可见，卡内文字排版和最近战绩表不变；多模式及名片任一失败路径
  回退原布局并仍返回战绩图片。
- [x] 不新增运行时依赖、配置、聊天参数或持久化数据；新增外部请求仍使用既有凭据最小化、
  受信 HTTPS 主机、大小限制、媒体超时与最多三次有界重试。
- [x] 新回归测试、`validate_plugin.py`、`ruff check .`、`pyright`、`pytest` 全部通过。
- [x] AstrBot 4.26.6 / Python 3.12.13 Docker import、isolated load、真实小药诊断、技改事件链
  与名剑名片增强/回退成图验证通过，临时容器均已清理。

### 2026-07-20 第四轮反馈

- [x] 超长技改正文无论由单个超长段落还是多个段落组成，都保留单图上限内可容纳的正文行；
  正文卡片存在可见文字且截断提示、页脚和图片高度上限同时有效。
- [x] 真实小药接口当前最长名称（含 `风语·上品亢龙散（外功）` 等夹具）最多占两行，短名称
  仍使用 20 px，所有文字不截断、不越界且闭合右括号不孤行。
- [x] 名剑赛季卡的当前积分、排名和 MVP 均保留在右半边且左边缘共线；段位、战绩及双列
  结构保持不变，三种模式卡片仍完整可读。
- [x] README 明确 AstrBot 4.26.6 同名 ZIP 上传与“更新”的差异，并说明独立 `plugin_data`
  字体目录不会因插件源码更新而被删除。
- [x] 新回归测试、`validate_plugin.py`、`ruff check .`、`pyright`、`pytest` 全部通过。
- [x] AstrBot 4.26.6 / Python 3.12.13 Docker import、isolated load 及新进程真实小药、技改正文
  和名剑战绩成图验证通过，临时容器均已清理。
- [x] 正式挂载容器通过官方 Dashboard API 仅重载 JX3Tools 到 v0.6.0；旧 handler 正常移除，
  新实例成功初始化，日志无本插件 traceback 或重复 handler，且未中断其他插件和平台连接。

### 2026-07-19 第三轮反馈

- [x] 新闻、公告、技改的列表流立即结束，使真实 WebChat 可在 10 秒内处理发起者的合法序号；
  选择流完整返回进度和正文图片且不再发送超时，完成、失败、替换、超时及终止均不残留任务。
- [x] 无相楼门派名与幽罗引使用同一主题色；长小药文本成图不出现单独占一行的右括号。
- [x] 奇遇记录保持三列，图标明显增大，并按“图标、触发时间、奇遇名称”的视觉重要性排序。
- [x] 名剑战绩赛季卡左对齐模式名，同一行显示当前积分；其余项目每行两项；最近战绩只显示
  `2V2`、`3V3`、`5V5` 等完整模式名，不退化成单个数字。
- [x] 名剑门派从注册表、别名搜索、帮助、README 和可调用指令中完全移除，不影响其他名剑功能。
- [x] 物品搜索有效 `view` 图片宽度接近内容框；物价能够解析当前真实 JX3API 响应并生成价格
  统计与近期记录，不返回“不完整读取响应”的通用错误。
- [x] 新回归测试、`validate_plugin.py`、`ruff check .`、`pyright`、`pytest` 全部通过。
- [x] AstrBot 4.26.6 / Python 3.12.13 Docker import、isolated load、正式挂载加载与真实只读
  冒烟通过，并保留文章事件链、物价真实响应和关键 Pillow 成图的用户可见证据。

### 2026-07-19 第二轮反馈

- [x] 月历长大战/战场夹具在 720 px 图片中完整换行、按周动态增高，无越界、重叠或截断。
- [x] 新闻同时覆盖旧式 `show-catid-id` 与当前 `announce/gg?id` URL，新闻、公告、技改在仿
  AstrBot 事件传播测试中均能在进度消息后继续返回正文图片；无输入时原回复流在 10 秒后可见
  超时提示，选择、替换和终止均清理等待状态。
- [x] 小药为无显式边框的 1+4 五列布局，门派/心法/小药均着色；奇遇为三列图标-名称-时间
  单元布局，等级 3 记录完全过滤。
- [x] 名剑战绩胜率标识、三列赛季卡、单行模式/心法、完整模式名和积分变化色符合反馈；名剑
  门派同一行展示并使用高饱和红绿；金价折线抗锯齿且最近点数值标签不重叠。
- [x] 今日/本周赤兔的 400“暂无数据”和空对象均输出明确纯文字，不显示通用查询失败。
- [x] 物品搜索先逐行输出全部名称，搜索和物价的有效 `view` 图片位于朱砂线与首个内容标题
  之间；无效图片静默省略；物价不再包含物品摘要。
- [x] 新回归测试、`validate_plugin.py`、`ruff check .`、`pyright`、`pytest` 全部通过。
- [x] AstrBot 4.26.6 / Python 3.12.13 Docker import、isolated load、正式挂载加载与真实只读
  冒烟通过，且文章事件链路和赤兔空数据路径有用户可见结果证据。

### 既有功能

- [x] 帮助统一使用“指令”，支持“全部”，只列出 27 个保留指令；6 个已移除指令不可调用。
- [x] 月历、行侠、科举、小药、阵眼的参数和专用排版完全符合本轮反馈，并覆盖边界测试。
- [x] 文章正文参数来自 URL，三类正文都可渲染；10 秒无输入会主动通知，状态和任务可靠清理。
- [x] 奇遇记录使用分组大表格和插件内固定图标；百战使用 10 列蛇形图和插件内固定首领头像。
- [x] 名剑战绩按官方字段语义生成统计与近期表格；已移除的名剑门派不再注册或展示。
- [x] 金价固定 15 条并生成多折线图；物价完成嵌套记录解析、统计和近期记录表格。
- [x] 名片有媒体专用超时/重试；物品搜索可靠嵌入官方图标；赤兔空数据使用明确文字提示。
- [x] 角色百战仅保留 5 类信息；解密和骚话/舔狗按要求输出可复制纯文字。
- [x] 所有图片副标题朱砂线按实际宽度动态绘制，新增表格/折线/蛇形布局在 720 px 下可读。
- [x] Token/Ticket 最小化注入、受控网络、大小限制、临时文件和后台任务均有失败路径测试。
- [x] `validate_plugin.py`、`ruff check .`、`pyright`、`pytest` 全部通过。
- [x] AstrBot 4.26.6 / Python 3.12.13 Docker import、isolated load、正式容器重载与代表性冒烟通过。

## Verification

1. `python <skill>/scripts/validate_plugin.py <plugin>`
2. `ruff check .`
3. `pyright`
4. `pytest`
5. `python <skill>/scripts/verify_plugin.py <plugin> --docker-import`
6. `python <skill>/scripts/verify_plugin.py <plugin> --isolated-load`
7. 正式 AstrBot 4.26.6 重载；检查依赖、字体、handler、session、临时文件与 traceback。
8. 实测免费文字、复杂图片、会员图表、名片直图、文章选择、超时/非发起者/重试路径。

## Verification evidence

### v0.7.2（GitHub 首次公开准备）

2026-08-01 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成以下非破坏性验证：

- 旧版备份服务的月历、小药、新闻、骚话与会员接口均以固定 `POST /data/...`、JSON 请求体返回
  业务码 200；凭据对比确认 Token 只放 JSON 即可通过，无需重复进入请求头。
- 完整真实只读冒烟为 0 failures，覆盖月历、行侠、科举、小药、三类文章正文与 AstrBot 事件链、
  金价、百战、物品搜索/物价、随机名片媒体、解密和赤兔空数据；主站旧图片 URL 经精确备份主机
  改写后通过相同安全下载链，随机名片成功读取 817,034 bytes。
- `validate_plugin.py --release --strict` 为 0 errors / 0 warnings；compile、Ruff、Pyright
  （0 errors / 0 warnings）、正式挂载目录 106 项测试和 `pip check` 全部通过。
- `pip-audit` 未发现已知漏洞；素材排除前 `detect-secrets` 对 133 个候选文件复扫为 0 results；
  最终候选清单缩减为 37 个文件，96 张 JX3BOX PNG、字体、大文件、本地 `.venv`、缓存、覆盖率和
  构建产物均被 `.gitignore` 排除。
- `verify_plugin.py --docker-import --isolated-load` 的 Docker import、Docker Ruff、Docker pytest
  （97 passed / 9 skipped）和隔离加载通过；跳过项为隔离副本未挂载私有字体。包装结果 `PARTIAL`
  仅表示宿主默认 Python 未安装三项质量工具，这些门禁已在匹配容器和固定 Pyright 版本分别通过。

### v0.7.1（第六轮名剑排版微调）

2026-07-20 至 2026-07-21 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成以下非破坏性验证：

- 用户先审阅真实数据的 +28 px 预览，随后要求再右移 10 px，并最终确认累计 +38 px 预览。
  最终代码使用固定 `ARENA_PROFILE_RIGHT_COLUMN_SHIFT = 38`，只由单模式名片布局的高度测量
  与绘制路径传入；普通多模式卡片及名片不可用回退路径继续使用默认 0 px。
- 坐标回归确认当前积分、排名和 MVP 的左边缘严格共线并相对基线右移 38 px；同一渲染回归
  记录名片布局偏移为 38、无名片回退为 0，模式名、段位、战绩和外框尺寸未改变。
- `validate_plugin.py` 为 0 errors，仅保留占位许可证的 2 个预期发布警告；Docker Ruff 通过，
  项目虚拟环境 Pyright 为 0 errors / 0 warnings，正式挂载目录 `pytest` 为 78 passed，唯一
  警告来自 AstrBot Core 的 Python 3.13 `audioop` 弃用提示。
- 正式挂载代码的真实名剑事件链为 0 failures：同一角色战绩、名片查询和本地成图通过，
  690,837-byte 名片进入渲染器并生成 720×1,305 PNG，图片尺寸与确认预览一致。
- 最终 `verify_plugin.py --docker-import --isolated-load` 的 Docker import、Docker ruff、
  Docker pytest（69 passed / 9 skipped）和隔离加载全部通过；9 个跳过项均为隔离容器未挂载
  私有字体。包装结果 `PARTIAL` 仅表示宿主 Python 未安装 ruff/pytest 且未识别项目内 Pyright，
  三项已分别实测；临时容器和容器内预览文件均已清理。

### v0.7.0（第五轮反馈完善）

2026-07-20 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成以下非破坏性验证：

- `validate_plugin.py` 为 0 errors，仅保留自用项目占位许可证的 2 个预期发布警告；
  Docker `ruff check .` 通过，项目虚拟环境 Pyright 为 0 errors / 0 warnings，正式挂载目录
  `pytest` 为 78 passed，唯一警告来自 AstrBot Core 的 Python 3.13 `audioop` 弃用提示。
- 真实小药第一次独立诊断遇到 JX3API 三次请求均超时，第二次在相同生产超时和重试边界下成功：
  接口总计返回 184 条；天罗诡道原始 8 条，其中 1 条完整 `名称（属性）` 重复，唯一项 7 条，
  插件渲染项也是 7 条且顺序、内容一致，生成 PNG 为 626,626 bytes。因此用户看到 7 项源于
  当前 API 仅提供 7 个唯一内容，不是插件的四列分块或 `max_items` 截断。
- 真实文章烟测为 0 failures：新闻、公告各读取 2 条，技改接口返回 18 条；三类正文和事件链
  全部成图，新闻/公告无 URL，技改在图片前发送规范化的所选官网 URL，选择完成后无错误超时。
- 使用反馈截图中同一角色身份完成真实单模式名剑全链路：战绩查询后复用名片接口，690,837-byte
  名片成功进入本地渲染器并生成 720×1,305 PNG。合成名片预览确认统计卡宽度约为内容区 60%，
  卡内原双列文字保持不变，右侧等高圆角图片填满原空旷区域，最近战绩表不受影响。
- `verify_plugin.py --docker-import --isolated-load` 的 Docker import、Docker ruff、Docker pytest
  （69 passed / 9 skipped）和隔离加载全部通过；9 个跳过项均为隔离容器未挂载私有字体。
  包装结果 `PARTIAL` 仅表示宿主 Python 未安装 ruff/pytest 且未识别项目内 Pyright，三项已分别
  实测。验证创建的临时容器已自动清理，最终只读检查无 `codex-astrbot` 残留容器。

### v0.6.0（第四轮反馈修复）

2026-07-20 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成以下非破坏性验证：

- `validate_plugin.py` 为 0 errors，仅保留自用项目占位许可证的 2 个预期发布警告；
  `ruff check .` 通过，Pyright 为 0 errors / 0 warnings，正式挂载目录为 73 passed，唯一警告
  来自 AstrBot Core 的 Python 3.13 `audioop` 弃用提示。
- 两类超长文章回归夹具均从失败转为通过；真实技改列表前 5 篇中最长正文为 26,024 字符，
  生成 720×50,000、11,473,150-byte PNG，底部仍有连续正文、明确截断提示和来源页脚。
- 真实小药响应返回 184 条外层数据、形成 183 个小药展示项；全部使用 20 px 且最多两行，
  生成 720×4,596、626,626-byte PNG，不再误报截断。另补多 chunk 高度测量回归，消除
  测量与绘制不一致造成的假截断。
- 名剑战绩三模式夹具生成 720×833、67,403-byte PNG；当前积分、排名和 MVP 均保留在右半边，
  左边缘共线，段位、战绩和双列结构未改变。
- 最终 `verify_plugin.py --docker-import --isolated-load` 的 Docker import、Docker ruff、
  Docker pytest（65 passed / 8 skipped）和隔离加载全部通过，临时容器已清理；包装器的
  `PARTIAL` 仅表示宿主 Python 未安装 ruff/pytest 且未识别项目内 Pyright，三项已单独实测。
- 新进程真实只读烟测为 0 failures：小药查询/成图通过；新闻、公告、技改的列表、正文图片和
  AstrBot 事件链全部通过且无错误超时。
- 正式容器通过官方 Dashboard API 仅重载本插件：v0.5.0 正常 `terminate()`，两个旧 handler
  明确移除，随后加载并初始化 v0.6.0；没有重启容器或中断其他插件/平台。正式 WebChat 烟测
  列表流结束耗时 0.005 秒，合法选择得到 2 条进度和正文图片且无错误超时，无选择时收到一次
  超时通知；测试会话清理成功，最近日志无本插件 ERROR、Exception 或 traceback。

### v0.5.0（第三轮反馈修复）

2026-07-19 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成：

- `validate_plugin.py` 为 0 errors；仅剩自用项目未选择公开许可证的 2 个预期发布警告；
  `ruff check .` 通过，`pyright 1.1.411` 为 0 errors / 0 warnings，正式挂载测试为 68 passed。
- 真实 WebChat 使用临时会话发出新闻列表后，最终重载复测中首个列表结果到 `end` 仅 0.006 秒；立即回复 `1`
  收到 2 条进度、1 张正文图片和 1 个 `end`，没有错误超时；故意不选择时只收到一次超时通知，
  会话、消息和附件均由 AstrBot 官方删除接口清理成功。
- 正式容器真实只读冒烟中，无相楼宴席返回 184 条并生成 681009-byte PNG；物品搜索返回
  20 条名称并生成 2294547-byte PNG；物价解析为 9 字段结构并生成 357293-byte PNG。
  实测还捕获一次物品图片 Content-Length 断流，现已让普通物品图复用 3 次有界重试并补回归测试。
- Pillow 视觉核验通过无相楼主题色与右括号换行、奇遇大图标三列、名剑赛季卡双列信息、
  完整 `3V3` 模式名和接近内容框宽度的物品图；名剑门派已从注册表与文档移除。
- `verify_plugin.py --docker-import --isolated-load` 的 Docker import、ruff、pytest（65 passed /
  3 skipped）与隔离加载全部通过；正式容器于 22:58 最终重载 v0.5.0，WebUI 就绪，烟测会话
  正常关闭且日志无本插件 ERROR、Exception 或 traceback。包装器的 PARTIAL 仅表示宿主 Python
  环境没有 ruff/pyright/pytest 命令，三项已分别实测通过。

### v0.4.0（第二轮反馈修复）

2026-07-19 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成：

- `validate_plugin.py`：0 errors；仅有自用项目未采用公开许可证的 2 个预期发布警告。
- 正式挂载目录 `ruff check .` 通过；本地 `pyright 1.1.411` 为 0 errors / 0 warnings；正式挂载
  目录 `pytest` 为 67 passed（仅 1 个 Python 标准库弃用警告）。
- `verify_plugin.py --docker-import --isolated-load`：插件契约、Python 编译、Docker import、
  Docker ruff、Docker pytest（64 passed / 3 skipped）及隔离加载全部通过；包装结果为 PARTIAL
  仅因宿主 Python 未安装 ruff/pyright/pytest 命令，正式挂载容器和本地虚拟环境的对应检查
  已实际通过。
- 修复后的凭据安全真实只读完整烟测失败数为 0，覆盖月历、小药、新闻/公告/技改正文、三类文章完整
  AstrBot 事件生成器链路、名剑门派、金价、百战、物品搜索名称与图片、物价、随机名片、
  解密和今日/本周赤兔空数据。
- 正式容器重启后的复测中，新闻/公告/技改正文、文章事件链和两类赤兔空数据均通过；月历
  首次遇到一次外部响应未完整读取，同一容器随后以 `curl` 收到 HTTP 200 / 8800 bytes，且
  正式插件客户端定点复测为 `calendar:PASS dict:2`，确认接口恢复且解析链路正常。
- Pillow 视觉核验通过月历动态增高、小药 1+4 五列、奇遇三列、名剑战绩/门派、金价抗锯齿
  标签和物品 `view` 图片位置；等级 3 奇遇在输出模型及成图中均不存在。
- 正式 AstrBot 容器于 20:47 重启，明确加载 `astrbot_plugin_jx3tools v0.4.0` 并完成初始化；
  WebUI 恢复就绪，启动日志中没有本插件的 ERROR、Exception 或 traceback。

### v0.3.0

2026-07-19 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成：

- `validate_plugin.py`：0 errors；仅有自用项目未采用公开许可证的 2 个预期发布警告。
- `ruff check .`：通过；`pyright 1.1.411`：0 errors / 0 warnings；`pytest`：59 passed。
- `verify_plugin.py --docker-import --isolated-load`：Docker import、Docker ruff、Docker pytest、
  隔离加载全部通过；包装结果为 PARTIAL 仅因宿主 Python 未安装对应命令行工具，已分别在
  正式容器和本地虚拟环境实际执行并通过。
- 正式 AstrBot 容器于 16:04 重启，明确加载 v0.3.0 并完成初始化；重启及烟测后日志中没有
  本插件的 ERROR、Exception 或 traceback。
- 凭据安全的真实只读烟测失败数为 0，覆盖月历、行侠、科举、小药、新闻/公告/技改正文、
  名剑门派、金价、百战、物品搜索图标、物价嵌套记录、随机名片媒体、解密及赤兔空数据。
- 实际 Pillow 视觉核验通过月历、奇遇记录、金价和百战；固定资源包含 60 个奇遇图标与
  36 个首领头像，运行时不依赖 JX3BOX 网络。

## Previous verification evidence

2026-07-18 在 AstrBot `4.26.6`、Python `3.12.13`、镜像摘要
`sha256:7546bddf1040419a455dd1ca683a5e9cf84436bbd85de17c7ac626d3af7affe4`
完成：

- `validate_plugin.py`：0 errors；仅保留自用项目未选择公开许可证的 2 个发布警告。
- `ruff check .`：通过；`pyright 1.1.411`：0 errors / 0 warnings。
- 正式挂载目录容器测试：48 passed；技能 Docker import 测试：47 passed / 1 skipped，
  唯一跳过项是隔离只读环境未提供私有字体文件。
- `verify_plugin.py --docker-import --isolated-load`：Docker import、Docker ruff、Docker pytest、
  隔离加载全部通过；包装结果为 PARTIAL 仅因宿主系统 Python 未安装 ruff/pyright/pytest，
  三项已分别在容器和本地隔离环境实际执行并通过。
- 正式容器重启后加载 v0.2.0，字体成功初始化，无本插件 traceback、重复 handler 或依赖错误。
- 真实只读冒烟通过：月历 30、小药、新闻、公告、技改正文、八卦、名剑门派、金价、诛恶、
  百战、马场、关隘、随机名片、解密、黑市、精确物价、物品搜索图标。
- 实际 Pillow 渲染通过：月历、小药、百战、关隘、解密、名剑门派、新闻/公告/技改正文和
  名片直图；全部使用本地字体，不调用远程 HTML renderer。
